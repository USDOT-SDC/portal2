import { CustomCredentialProvider } from './custom-credential-provider';

describe('CustomCredentialProvider', () => {
  it('should create an instance', () => {
    expect(new CustomCredentialProvider()).toBeTruthy();
  });
});
