export const environment = {
  production: ${production},
  stage: '${stage}',
  build: '${build}',
  resource_urls: {
    portal: '${portal_url}',
    portal_api: '${portal_api_url}',
    guacamole: '${guacamole_url}',
    sftp: '${sftp_url}',
    sub1: '${sub1_url}',
    sub2: '${sub2_url}',
  }
};
